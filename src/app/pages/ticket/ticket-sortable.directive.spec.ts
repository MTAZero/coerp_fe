import { TicketSortableDirective } from './ticket-sortable.directive';

describe('TicketSortableDirective', () => {
  it('should create an instance', () => {
    const directive = new TicketSortableDirective();
    expect(directive).toBeTruthy();
  });
});
